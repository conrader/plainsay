{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hud-events",
  "description": "Allow the read-only HUD to receive dictation status updates",
  "windows": ["hud"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
